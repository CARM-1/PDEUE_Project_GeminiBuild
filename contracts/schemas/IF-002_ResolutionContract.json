{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IF-002_ResolutionContract",
  "type": "object",
  "properties": {
    "contract_id": { "type": "string" },
    "event_id": { "type": "string" },
    "station_id": { "type": "string" },
    "metric": { "type": "string" },
    "unit": { "type": "string" }
  },
  "required": ["contract_id", "event_id", "station_id", "metric", "unit"]
}
